family: 16h5            # tag family, one of 16h5, 25h7, 25h9, 36h10, 36h11, 36artoolkit
size: 0.0545             # tag edge size in meter
max_hamming: 0          # maximum allowed hamming distance (corrected bits)
z_up: true              # rotate about x-axis to have Z pointing upwards

# see "apriltag.h" for more documentation on these optional parameters
decimate: 1.0           # decimate resolution for quad detection
blur: 0.0               # sigma of Gaussian blur for quad detection
refine-edges: 1         # snap to strong gradients
threads: 1             # number of threads
debug: 0                # write additional debugging images to current working directory

# optional list of tags
tag_ids: [8]            # tag ID
tag_frames: [purple_cube]  # optional frame name
tag_sizes: [0.0545]   # optional tag-specific edge size
